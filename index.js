var k=["RED", "AND", "WHITE"]

for (var i=0;i<k.length;i++){
    if(k[i]=="RED")
    {
        console.log(k[i])
    }
    else if(k[i]=="AND")
    {
        console.log(k[i].toString().toLocaleLowerCase())
    }
    else if(k[i]=="WHITE")
    {
        console.log(k[i])
    }
}